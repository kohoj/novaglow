{
  "name": "novaglow-root",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm --filter @novaglow/web dev",
    "cli": "npx tsx packages/cli/src/index.ts"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["@napi-rs/canvas", "esbuild", "sharp"]
  }
}
